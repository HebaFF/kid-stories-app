/**
 * Shared prompt fragment used by every page's `illustration` prompt.
 *
 * Keep it in every prompt. Without it, pages drift into different styles and a
 * story stops looking like one book. `no text, no lettering` matters most:
 * image models produce garbled pseudo-Arabic that looks alarming to a parent.
 */
export const STYLE =
  'soft watercolour children’s book illustration, warm Egyptian palette, gentle rounded shapes, thick soft outlines, no text, no lettering';
